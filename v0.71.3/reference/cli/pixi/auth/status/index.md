# [pixi](../../) [auth](../) status

Show stored authentication entries and non-secret token metadata

## Usage

```text
pixi auth status [OPTIONS]
```

## Options

- [`--verbose (-v)`](#arg---verbose) : Show endpoint URLs, client ID, and other IdP-introspection fields that are only useful for debugging

## Examples

```shell
pixi auth status
```
