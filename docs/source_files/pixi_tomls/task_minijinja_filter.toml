
[workspace]
channels = ["conda-forge"]
name = "workspace-name"
platforms = ["linux-64"]
# --8<-- [start:tasks]
[tasks]
# Convert argument to uppercase
upper = { cmd = "echo {{ text | upper }}", args = ["text"] }
# --8<-- [end:tasks]
