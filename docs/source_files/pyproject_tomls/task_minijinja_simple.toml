
[tool.pixi.workspace]
channels = ["conda-forge"]
name = "workspace-name"
platforms = ["linux-64"]
# --8<-- [start:tasks]
[tool.pixi.tasks]
greet = { cmd = "echo Hello, {{ name }}!", args = ["name"] }
# --8<-- [end:tasks]
