[project]
dependencies = ["rich"]
name = "rich-example"
requires-python = ">= 3.11"
version = "0.1.0"

[project.scripts]
rich-example-main = "rich_example:main"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]
