--8<-- [start:example]

## Examples

```shell
pixi shell
pixi shell --manifest-path ~/myproject/pixi.toml
pixi shell --frozen
pixi shell --locked
pixi shell --environment cuda
exit
```

--8<-- [end:example]
