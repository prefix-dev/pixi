[project]
name = "bad-conda-pypi-map"
channels = ["conda-forge"]
platforms = ["linux-64"]
# `true` is not a valid channel entry; only a string, a table or `false` is.
conda-pypi-map = { conda-forge = true }
