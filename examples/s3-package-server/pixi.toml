[project]
channels = ["s3://s3-forge/channel", "conda-forge"]
name = "s3-package-server"
platforms = ["osx-arm64"]

[project.s3-options]
endpoint-url = "http://localhost:9000"
region = "eu-central-1"
force-path-style = true

[dependencies]
test-package = "*"
# my-webserver = "*"
