[project]
name = "pypi-find-links"
version = "0.1.0"
description = "Add a short description here"
authors = ["Tim de Jager <tim@prefix.dev>"]
channels = ["conda-forge"]
platforms = ["osx-arm64"]

[tasks]

[pypi-options]
flat-indexes = [{ path = "./links" }]

[dependencies]
python = "3.12"

[pypi-dependencies]
requests = "==2.31.0"
