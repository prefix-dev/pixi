[project]
name = "lidar"
version = "0.1.0"
# requires-python = "<3.12"
dependencies = [
  "matplotlib",
  "numpy",
  "nuscenes-devkit",
  "requests",
  "rerun-sdk",
]
readme = "README.md"

[project.scripts]
lidar = "lidar.__main__:main"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]
