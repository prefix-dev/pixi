[project]
dependencies = [
  "matplotlib",
  "numpy",
  "nuscenes-devkit",
  "requests",
  "rerun-sdk",
]
name = "nuscenes_dataset"
readme = "README.md"
version = "0.1.0"

[project.scripts]
nuscenes_dataset = "nuscenes_dataset.__main__:main"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]
