[package]
description = "Rust implementations of particle emitters and modifiers, exposed to Python via PyO3"
edition = "2021"
name = "particle_rs"
version = "0.1.0"

[lib]
crate-type = ["cdylib"]
name = "particle_rs"

[dependencies]
pyo3 = { version = "0.28", features = ["extension-module"] }
