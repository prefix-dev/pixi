[project]
name = "docker-build"
version = "0.1.0"
description = "Add a short description here"
authors = ["Ruben Arts <ruben@prefix.dev>"]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tasks]

[dependencies]
