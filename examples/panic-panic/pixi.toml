[workspace]
channels = ["conda-forge"]
name = "test"
platforms = ["osx-64"]
# platforms = ["linux-64"]
version = "0.1.0"

[dependencies]
python = "3.9"

[pypi-dependencies]
# kahypar = "==1.0"
# panic-panic = { path = "." }
sdist = "*"
