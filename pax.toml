[project]
name = "pax"
version = "0.1.0"
authors = ["Bas Zalmstra <bas@prefix.dev>", "Wolf Vollprecht <wolf@prefix.dev>", "Tim De Jager <tim@prefix.dev>", "Ruben Arts <ruben@prefix.dev>"]
channels = ["conda-forge"]
platforms = ["linux-64", "win-64", "osx-64", "osx-arm64"]

[commands]
custom_command = "echo hello_world"
style = "pre-commit run --all"
test = "cargo test"

[dependencies]
pre-commit = "*"
