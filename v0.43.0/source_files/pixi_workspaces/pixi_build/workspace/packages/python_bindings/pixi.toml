[package]
name = "python_bindings"
version = "0.1.0"

[package.build]
backend = { name = "pixi-build-cmake", version = "0.1.*" } # (5)!

[package.host-dependencies]
cmake = "3.20.*"
nanobind = "2.4.*"
python = "3.12.*"
