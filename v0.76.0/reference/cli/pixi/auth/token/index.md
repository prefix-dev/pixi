# [pixi](../../) [auth](../) token

Print the stored authentication token for a given host

## Usage

```text
pixi auth token <HOST>
```

## Arguments

- [`<HOST>`](#arg-%3CHOST%3E) : The host to print the stored token for (e.g. prefix.dev)

  ```
  **required**: `true`
  ```
