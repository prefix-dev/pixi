[project]
name = "separate-workspace"
version = "1.0.0"

[tool.pixi.workspace]
channels = []
description = "Separate workspace description"
license = "Apache-2.0"
platforms = []
preview = ["pixi-build"]
