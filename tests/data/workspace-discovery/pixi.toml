[workspace]
channels = []
name = "workspace-discovery"
platforms = []
preview = ["pixi-build"]

[package]
# name is explicitly inherited from the workspace in the same folder.
name = { workspace = true }
version = "0.1.0"

[package.build]
backend = { name = "bla", version = "*" }
