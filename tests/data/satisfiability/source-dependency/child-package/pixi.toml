[package]
name = "child-package"
version = "0.1.0"

[package.build]
backend = { name = "in-memory", version = "*" }

# A comment shouldn't change the project model and therefore, shouldn't change the hash
