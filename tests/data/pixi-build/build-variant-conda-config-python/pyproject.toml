[project]
description = "Variant conda config test package built with pixi-build-python."
name = "variant-conda-config-python"
requires-python = ">=3.11"
version = "1.0.0"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.hatch.build.targets.wheel]
packages = ["variant_conda_config_python"]
