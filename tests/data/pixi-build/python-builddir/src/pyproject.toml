[project]
name = "simple-meson-app"
requires-python = ">= 3.11"
scripts = { simple-meson-app = "simple_meson_app:main" }
version = "0.1.0"

[build-system]
build-backend = "mesonpy"
requires = ["meson-python"]
