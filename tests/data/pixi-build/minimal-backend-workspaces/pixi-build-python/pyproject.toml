[project]
dependencies = ["rich"]
name = "simple-app"
requires-python = ">= 3.11"
scripts = { simple-app = "simple_app:main" }
version = "0.1.0"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]
