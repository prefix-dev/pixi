[package]
name = "package_b"
version = "0.1.0"

[package.build]
backend = { name = "in-memory", version = "*" }

# The passthrough backend produces noarch outputs, and a noarch consumer only
# receives the noarch bucket.
[package.run-exports.noarch]
package_b = { path = "." }
