[package]
name = "package-a"
version = "0.1.0"

[package.build]
backend = { name = "in-memory", version = "*" }

[package.host-dependencies]
package-b = { path = "../package-b" }

[package.build.config]
package = "package-a-0.1.0-h4616a5c_0.conda"
