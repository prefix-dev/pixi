[project]
name = "local-library"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Tim de Jager", email = "tim@prefix.dev" }
]
requires-python = ">=3.11"
dependencies = [
    "local-library2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
local-library2 = {path = "../local-library2", editable = true}
