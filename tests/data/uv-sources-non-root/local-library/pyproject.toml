[project]
authors = [{ name = "Tim de Jager", email = "tim@prefix.dev" }]
dependencies = ["local-library2"]
description = "Add your description here"
name = "local-library"
readme = "README.md"
requires-python = ">=3.11"
version = "0.1.0"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.uv.sources]
local-library2 = { path = "../local-library2", editable = true }
