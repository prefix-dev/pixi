[workspace]
channels = []
preview = ["pixi-build"]

[package]
name = "simple"
version = "0.1.0"

[package.build]
backend = { name = "pixi_build_backend", version = "*" }
channels = ["https://prefix.dev/pixi-build-backends"]
