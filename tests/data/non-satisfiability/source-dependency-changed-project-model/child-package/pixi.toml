[package]
name = "child-package"
# The version used to be 0.1.0, that should change the project model, and therefore the hash
version = "0.1.2"

[package.build]
backend = { name = "pixi-build-cmake", version = "0.1.*" }
