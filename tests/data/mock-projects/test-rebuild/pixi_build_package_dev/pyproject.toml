[project]
authors = [{ name = "Julian Hofer", email = "julianhofer@gnome.org" }]
dependencies = []
name = "pixi_build_package_dev"
requires-python = ">= 3.11"
scripts = { pixi-build-package-dev-main = "pixi_build_package_dev:main" }
version = "0.1.0"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]
