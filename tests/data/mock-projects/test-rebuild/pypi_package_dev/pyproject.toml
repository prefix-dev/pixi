[project]
authors = [{ name = "Julian Hofer", email = "julianhofer@gnome.org" }]
dependencies = []
name = "pypi_package_dev"
requires-python = ">= 3.11"
scripts = { pypi-package-dev-main = "pypi_package_dev:main" }
version = "0.1.0"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]
