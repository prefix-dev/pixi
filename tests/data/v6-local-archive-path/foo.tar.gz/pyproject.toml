[project]
dependencies = []
name = "foo"
requires-python = ">=3.11"
version = "23.42"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.hatch.build]
include = ["src"]
targets.wheel.packages = ["src/foo"]
