package:
  name: cuda-arch
  version: 0.1.0
build:
  number: 1
  string: sm100_1
  # __cuda_arch is a linux/CUDA concept; keep the fixture linux-only.
  skip: not linux
requirements:
  run:
    - __cuda_arch >= 10
