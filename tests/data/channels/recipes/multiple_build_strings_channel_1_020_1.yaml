recipe:
  name: multiple-build-strings-channel
  version: 1.0.0

context:
  version: 0.2.0
outputs:
  - package:
      name: package
      version: ${{ version }}
    build:
      number: 1

  - package:
      name: package2
      version: ${{ version }}

    build:
      number: 1
