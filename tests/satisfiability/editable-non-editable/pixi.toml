[project]
name = "editable-non-editable"
channels = ["conda-forge"]
platforms = ["win-64"]

[dependencies]
python = "3.12.*"

[pypi-dependencies]
foo = { path = "./foo", editable = true }
bar = { path = "./bar", editable = true }
