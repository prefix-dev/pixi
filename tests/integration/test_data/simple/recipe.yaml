context:
  version: "1.2.7"

package:
  name: life_package
  version: ${{ version }}

source:
  path: .

build:
  noarch: generic
  script:
    - mkdir -p $PREFIX/bin
    - cp life.v127.com $PREFIX/bin/life.com
    - if: unix
      then:
        - chmod +x $PREFIX/bin/life.com
